<p id="notice"><%= notice %></p>
<h1>Welcome to Nolan's Online Book Store</h1>
<br>
<h2><%= link_to 'Authors', authors_path %> |
<%= link_to 'Categories', categories_path %> | <%= link_to 'Users', users_path %><h2>
<br>
<br>
<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>Book Name</th>
      <th>Author</th>
      <th>Category</th>
      <th>Price</th>
      <th>Book Cover</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.bookname %></td>
        <td><%= book.author.authorname %></td>
        <td><%= book.category.categoryname %></td>
        <td><%= book.price %></td>
        <td><%= image_tag book.bookcover, :size=> "100x150" %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= will_paginate @books %>
<%= link_to 'New Book', new_book_path %>

